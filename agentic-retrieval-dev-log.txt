2026-04-17 AR-P1-T1/T2: extended v1 artifact types for cleaned intent + retrieval recommendations; runtime validators and fixtures updated; test-only callers patched for new required fields.

===== 2026-04-17T21:09:58Z | opus implement =====
Both active tasks are complete. Summary:

**AR-P1-T1 — types**: Extended `src/artifacts/types.ts`:
- `IntentCaptureV1`: added required `cleaned_user_intent` and optional `intent_file_refs` (with `IntentFileRef`/`IntentFileRefSource`).
- `RetrievalIndexV1`: added required `strategy_summary`, `scout_terms`, `recommended_evidence` (with `RetrievalRecommendedEvidence`, `RecommendedEvidenceFile`, `RecommendedEvidenceSpan`).
- `RetrievalFile`: added `selection_tier`, `selection_reason`, `default_evidence_mode` (with `RetrievalSelectionTier`, `RetrievalDefaultEvidenceMode`).
- `RetrievalSymbol`: added `selected_by_default`, `default_neighbor_lines`, `selection_reason`.

**AR-P1-T2 — schemas + fixtures + tests**: 
- `src/artifacts/schemas.ts`: validators for all new fields (union-value checks for source/tier/mode, nested validation for `recommended_evidence` spans).
- `tests/fixtures/sample-artifacts/intent-capture-v1.json` and `retrieval-index-v1.json`: updated with new shape (selected + reserve file example, recommended evidence payload, intent_file_refs).
- `tests/artifacts/schemas.test.ts`: added negative tests for missing cleaned_user_intent, bad intent_file_refs, missing strategy/scout/recommended_evidence, invalid selection_tier and default_evidence_mode, and malformed recommended_evidence spans.

**Caller coherence**: Updated producers to supply safe defaults so the codebase compiles:
- `src/conductor/stage-1.ts`: populates `cleaned_user_intent` (defaults to verbatim; Phase 2 will refine).
- `src/retriever/normalize.ts`: fills new retrieval defaults (`strategy_summary: ''`, `scout_terms: []`, `recommended_evidence: empty`, per-file `selection_tier: 'selected'`/`default_evidence_mode: 'summary'`, per-symbol zeroed defaults).
- Test construction sites in 9 files updated to match new shapes.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 378 pass / 2 fail — both failures (`tests/retriever/prompt.test.ts`) are pre-existing ("Tagged files:" vs "User-tagged files:") and unrelated to this work.

**Nothing unfinished** within the scope of AR-P1-T1 and AR-P1-T2.

===== 2026-04-17 | opus implement AR-P2 =====
All three Sprint 2 tasks complete.

**AR-P2-T1 — intent-files helper**: `src/util/intent-files.ts` already provided `extractIntentFiles` and `buildRestatementContext` with bounded byte/line budgets. Added `toIntentFileRefs()` to convert parsed files into `IntentFileRef[]` matching the artifact schema.

**AR-P2-T2 — Stage 1 wiring**:
- `RestateFunction` signature now takes `{ cleanedIntent, contextBlock? }` instead of a bare intent string.
- `Stage1Controller.captureIntent` accepts an options object (`cleanedIntent`, `intentFileRefs`, `restatementContext`) and persists `cleaned_user_intent` + `intent_file_refs` on the capture artifact. Context block is retained in-memory for restatement and re-used across correction turns.
- `submitApproval` correction path preserves `intent_file_refs` and tagged files from the prior capture (corrections are user-typed text with no inline `<file>` blocks).
- `extensions/conductor-extension.ts`: `runPipelineFromIntent` now runs `buildRestatementContext(initialIntent, repoRoot)` before capture, passes cleaned intent + tagged files + refs to `captureIntent`, and hands the cleaned intent + bounded context block to `restateWithModel`. No new general repo-reading capability introduced — only files explicitly embedded in the initial user input are read (and only through the existing helper).

**AR-P2-T3 — downstream cleaned-intent**:
- `src/conductor/expansion.ts` stores `capture.cleaned_user_intent` in `ExpansionInputV1.user_intent_verbatim` so expansion prompts are no longer diluted by raw `<file>` blocks.
- `src/services/retrieval-dispatch.ts` passes `capture.cleaned_user_intent` to `assembleRetrieverPrompt`.
- `src/retriever/prompt.ts`: documented that callers must pass the cleaned intent; the prompt already refrains from embedding verbatim text in the query body.

**Test updates**: `tests/interaction/stage-1.test.ts` and `tests/interaction/expansion-protocol.test.ts` `echoRestate` stubs updated to destructure `{ cleanedIntent }` per the new `RestateFunction` shape.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 378 pass / 2 fail — both failures remain the pre-existing `tests/retriever/prompt.test.ts` "Tagged files:" vs "User-tagged files:" mismatch unrelated to this work.

**Nothing unfinished** within the scope of AR-P2-T1/T2/T3.

===== 2026-04-17T21:17:06Z | opus implement =====
All three Sprint 2 tasks are complete.

## Summary

**AR-P2-T1** — Intent-files helper
- `src/util/intent-files.ts`: helper already implemented with bounded byte/line budgets. Added `toIntentFileRefs()` to convert parsed files into `IntentFileRef[]` matching the v1 artifact schema.

**AR-P2-T2** — Stage 1 wiring
- `src/conductor/stage-1.ts`: `RestateFunction` now takes `{ cleanedIntent, contextBlock? }`. `captureIntent` accepts an options object (cleaned intent, file refs, restatement context) and persists `cleaned_user_intent` + `intent_file_refs`. Context is retained for corrections.
- `extensions/conductor-extension.ts`: `runPipelineFromIntent` calls `buildRestatementContext` before capture; restatement model receives cleaned intent + bounded file context. No new general repo-reading capability.

**AR-P2-T3** — Cleaned intent downstream
- `src/conductor/expansion.ts`: expansion input now carries `capture.cleaned_user_intent`.
- `src/services/retrieval-dispatch.ts`: retriever prompt assembled from cleaned intent.
- `src/retriever/prompt.ts`: interface doc clarifies that callers pass cleaned intent.

**Tests updated**: `tests/interaction/stage-1.test.ts` and `tests/interaction/expansion-protocol.test.ts` stubs adapted to new `RestateFunction` shape.

**Verification**: `tsc --noEmit` clean. `bun test` 378 pass / 2 fail — both failures are the pre-existing `tests/retriever/prompt.test.ts` "Tagged files:" mismatch noted in the prior dev log, unrelated to this work.

**Files modified**: `src/util/intent-files.ts`, `src/conductor/stage-1.ts`, `src/conductor/expansion.ts`, `src/services/retrieval-dispatch.ts`, `src/retriever/prompt.ts`, `extensions/conductor-extension.ts`, `tests/interaction/stage-1.test.ts`, `tests/interaction/expansion-protocol.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`. Nothing unfinished within the active-task scope.

===== 2026-04-17 | opus implement AR-P3 =====
All three Sprint 3 tasks complete.

**AR-P3-T1 — scout module + curated terms**: New `src/retriever/scout.ts` exports `buildScoutTerms` and `runScout`. Terms are deterministic, weighted by provenance (focus=4, tag=3, restatement=2, intent=1), deduped with origin aggregation, stop-word filtered, and capped at MAX_TERMS=24. Tokenization is shared by splitting camelCase + `[._/-]` separators. The scout is the only file-reading retrieval path in Phase 3 and is separated from the later agent loop.

**AR-P3-T2 — ranked candidates, tiers, role hints**: `runScout` discovers files deterministically (sorted walk), scores each with weighted term/symbol/path matching, and splits results into `selected` (<=8) and `reserve` (<=4) tiers. Each `ScoutCandidate` carries: stable rationale string, `ScoutFileRole` (`tagged` | `entry-point` | `config` | `doc` | `test` | `implementation` | `unknown`), `RetrievalDefaultEvidenceMode` hint (short tagged files → whole_file; strong symbol score → spans; docs/config → summary; otherwise summary/summary+ast), top symbol hints, imports, AST skeleton, and lineCount. Tagged files get a +50 boost and always win ranking ties.

**AR-P3-T3 — dispatch wiring + deterministic tests**: `src/services/retrieval-dispatch.ts` now runs `runScout` explicitly before the retriever worker and the prompt assembler (the scout result is injected into both so no re-scanning happens). `src/retriever/worker.ts` reduced to orchestration: `runRetrieverWorker` either uses an injected scout or runs one, then `scoutToRawOutput` converts candidates into `RawRetrievalOutput` with per-file `selection_tier`, `selection_reason`, `default_evidence_mode`, plus top-level `strategy_summary` and `scout_terms`. `src/retriever/normalize.ts` propagates those new raw fields (with validated coercion to the v1 union types) into the stored `retrieval-index-v1` artifact. `src/retriever/prompt.ts` now shares term generation with the scout via `buildScoutTerms` and accepts a `scoutTerms` pass-through.

**Tests added/updated**:
- `tests/retriever/scout.test.ts` (new, 14 tests): weight ordering by origin, origin aggregation, stop-word exclusion, determinism, narrow bounded output, tagged-file role+boost, retrieval-focus steering, rationale/evidence-mode population, selected/reserve exclusivity, no-raw-body leak, strategy summary shape, cross-file hints, and a "narrower than the old 12-file heuristic default" guard on a 20-file repo.
- `tests/retriever/retrieval-index.test.ts`: dispatch now asserts that `strategy_summary` and `scout_terms` propagate into the stored artifact, that every file has a valid tier/mode, that the selected set is <=8 and <12, and that a tagged file is promoted into the selected tier with a "tagged" selection_reason.

**Side effect**: `src/retriever/prompt.ts`'s "User-tagged files:" label is now "Tagged files:", matching the test expectations from `tests/retriever/prompt.test.ts`. The two pre-existing failures noted in earlier dev logs are now resolved.

**Files modified**: `src/retriever/scout.ts` (new), `src/retriever/worker.ts`, `src/retriever/normalize.ts`, `src/retriever/prompt.ts`, `src/services/retrieval-dispatch.ts`, `tests/retriever/scout.test.ts` (new), `tests/retriever/retrieval-index.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 395 pass / 0 fail (up from 378 pass / 2 fail). Nothing unfinished within the AR-P3-T1/T2/T3 scope.

===== 2026-04-17T21:29:16Z | opus implement =====
All three Sprint 3 tasks complete. Summary:

**AR-P3-T1** — New `src/retriever/scout.ts` with deterministic `buildScoutTerms` (weighted by provenance: focus=4, tag=3, restatement=2, intent=1, stop-word filtered, capped at 24) and `runScout` that sorts filesystem walks for determinism.

**AR-P3-T2** — Scout emits `selected` (≤8) and `reserve` (≤4) tiers, each with stable rationale, `ScoutFileRole`, `RetrievalDefaultEvidenceMode` hint, top symbol hints, and AST skeleton. Tagged files get a +50 boost into `selected`.

**AR-P3-T3** — `retrieval-dispatch.ts` now runs `runScout` before the worker, passes the scout result into both the prompt and worker, and the new scout-authored fields (`strategy_summary`, `scout_terms`, per-file `selection_tier`/`selection_reason`/`default_evidence_mode`) flow through `worker.ts`'s `scoutToRawOutput` and `normalize.ts` into the stored artifact. Added `tests/retriever/scout.test.ts` (14 tests) and extended `tests/retriever/retrieval-index.test.ts`.

**Files modified**: `src/retriever/scout.ts` (new), `src/retriever/worker.ts`, `src/retriever/normalize.ts`, `src/retriever/prompt.ts`, `src/services/retrieval-dispatch.ts`, `tests/retriever/scout.test.ts` (new), `tests/retriever/retrieval-index.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 395 pass / 0 fail (up from 378 pass / 2 fail — fixing the "User-tagged files:" mismatch was a side benefit). Nothing unfinished within Phase 3 scope.

===== 2026-04-17 | opus implement AR-P4 =====
All three active Sprint 4 tasks complete (T1, T2, T3). Sprint 4 introduces a bounded, model-driven retriever agent behind the scout.

**AR-P4-T1 — protocol + prompts**: New `src/retriever/agent-types.ts` defines the typed round contract:
- `RetrievalAction` discriminated union: `read_file` (window or full), `search_content` (case-insensitive literal), `search_paths` (substring), `follow_imports` (relative specifier resolution).
- `RetrievalObservation` mirror union with success/truncated/error outcomes.
- `AgentRoundOutput` = `{ status: 'continue'; actions[] }` | `{ status: 'stop'; recommendation }`.
- `AgentFinalRecommendation` shape matches `RawRetrievalOutput` selection fields (path, tier, default_evidence_mode, selection_reason, symbol selections).
- `AgentLimits` with defaults: `maxRounds=3`, `maxActionsPerRound=4`, `maxFileReads=12`, `maxLinesPerRead=200`, `maxBytesPerRead=16*1024`, `maxObservationBudgetBytes=256*1024`.
- `AgentModelCallback` signature `({ systemPrompt, userPrompt, round }) => Promise<string>` — pi-host-agnostic.

New `src/retriever/agent-prompt.ts` exports `RETRIEVER_AGENT_SYSTEM_PROMPT` (constrains to single JSON object, enumerates action schemas, recommendation schema, bounded-behavior rules), plus `buildInitialRoundPrompt` and `buildSubsequentRoundPrompt(forceStop?)` that render scout seed + prior observations.

**AR-P4-T2 — deterministic executor**: New `src/retriever/executor.ts`:
- `createExecutorState(repoRoot, limits)` holds lazy repo index (sorted source files), read counter, observation-byte budget.
- `resolveInsideRepo` rejects any path that escapes `repoRoot` (absolute or `..` tricks).
- `executeReadFile` respects `start_line`/`end_line` window, caps lines at `maxLinesPerRead`, truncates at `maxBytesPerRead`, and charges `maxFileReads`.
- `executeSearchContent` case-insensitive literal with ≤4 hits/file and ≤25 total hits (from `maxContentSearchHits`).
- `executeSearchPaths` substring match against the lazy repo index (cap 25 matches).
- `executeFollowImports` resolves relative specifiers with `.ts`/`.tsx`/`.js`/`.mjs`/`.cjs` + `/index.*` candidates, marks unresolved ones.
- `executeActions(state, actions)` runs actions sequentially, produces a `RoundObservationList`, and short-circuits when the observation-byte budget would be exceeded.

**AR-P4-T3 — bounded loop + injection**: New `src/retriever/agent.ts`:
- `runRetrieverAgent({ repoRoot, intent, scout, model, limits })` drives the round loop.
- Per round: assemble prompt → `callModelSafely` → `parseRoundOutput` (strips code fences, extracts first `{…}` block) → if `stop` persist recommendation & break; else execute actions, record trace, continue.
- Final round forces `forceStop=true`. On parse/model error, falls through to `fallbackRecommendation` synthesized from the scout seed.
- `sanitizeRecommendation` normalizes all file paths to repo-relative and drops any that escape `repoRoot`.
- Telemetry: `roundsExecuted`, `actionsExecuted`, `fileReadsExecuted`, `observationBytes`, `stopReason` (`agent_stopped` | `round_cap` | `action_cap` | `model_error` | `parse_error`).

`src/retriever/worker.ts` rewritten for orchestration:
- `RetrieverWorkerInput` gains optional `model?: AgentModelCallback` and `agentLimits?: Partial<AgentLimits>`.
- New `runRetrieverWorkerDetailed(input) → { raw, scout, agent }`. Without a model → scout-only passthrough. With a model → `runRetrieverAgent`; agent selections overlay onto scout structural data (AST skeleton, imports, symbol metadata retained; tier/mode/reason/symbols overridden by agent). Empty agent recommendation falls back to scout output.
- Legacy `runRetrieverWorker` preserved as a `runRetrieverWorkerDetailed` wrapper for backward compatibility.

`src/services/retrieval-dispatch.ts` adds `RetrievalDispatchOptions` with `retrieverAgentModel` and `retrieverAgentLimits`, calls `runRetrieverWorkerDetailed`, and appends agent rounds + stopReason to the success message.

`extensions/conductor-extension.ts` adds `makeRetrieverAgentModel(ctx)` that adapts `getModelText(systemPrompt, userPrompt, ctx)` into an `AgentModelCallback`, wired into `runRetrievalStage` via `{ retrieverAgentModel: makeRetrieverAgentModel(ctx) }`. Boundary isolation verified: grep for `pi-ai|pi-coding-agent` in `src/` finds nothing.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 395 pass / 0 fail (existing coverage continues to pass because the worker falls back to scout-only when no model is injected; dedicated agent-loop tests are the AR-P4-T4 scope and out of active-task scope here).

**Files modified**: `src/retriever/agent-types.ts` (new), `src/retriever/agent-prompt.ts` (new), `src/retriever/executor.ts` (new), `src/retriever/agent.ts` (new), `src/retriever/worker.ts` (rewritten for agent integration), `src/services/retrieval-dispatch.ts` (options + agent telemetry), `extensions/conductor-extension.ts` (model callback factory + wiring), `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`. Nothing unfinished within the AR-P4-T1/T2/T3 scope — AR-P4-T4 (agent tests) and AR-P4-T5 remain `planned` as expected.

===== 2026-04-17T21:42:57Z | opus implement =====
Sprint 4 active tasks (AR-P4-T1, T2, T3) are complete.

**What changed**
- **AR-P4-T1** — New `src/retriever/agent-types.ts` (round contracts, `AgentLimits` defaults: 3 rounds / 4 actions / 12 reads / 200 lines / 16KB / 256KB budget, `AgentModelCallback` signature) and `src/retriever/agent-prompt.ts` (system prompt + initial/subsequent round builders with `forceStop`).
- **AR-P4-T2** — New `src/retriever/executor.ts` with `resolveInsideRepo` path safety, bounded `read_file` / `search_content` / `search_paths` / `follow_imports` executors, lazy repo index, and observation-byte budget enforcement.
- **AR-P4-T3** — New `src/retriever/agent.ts` drives the bounded round loop with robust JSON parsing, scout-seeded fallback on model/parse error, path sanitization, and telemetry (`stopReason`). `src/retriever/worker.ts` rewritten to overlay agent selections onto scout structural data; `src/services/retrieval-dispatch.ts` takes a `retrieverAgentModel` option; `extensions/conductor-extension.ts` injects `makeRetrieverAgentModel(ctx)` at the extension edge. `src/` has no pi-host imports.

**Files modified**: `src/retriever/agent-types.ts` (new), `src/retriever/agent-prompt.ts` (new), `src/retriever/executor.ts` (new), `src/retriever/agent.ts` (new), `src/retriever/worker.ts`, `src/services/retrieval-dispatch.ts`, `extensions/conductor-extension.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Verification**: `bunx tsc --noEmit` clean; `bun test` 395 pass / 0 fail.

**Not done (by design)**: AR-P4-T4 (agent-loop tests) and AR-P4-T5 remain `planned` — out of active-task scope.

===== 2026-04-17T21:54Z | opus implement AR-P4-T4 + AR-P5-T1 =====
Two active tasks complete: retriever-agent test suite and recommended-evidence normalization.

**AR-P5-T1 — structural recommended_evidence in normalization**
`src/retriever/normalize.ts` now builds `recommended_evidence` from the normalized file set:
- `RawRetrievalSymbol` gained `selected_by_default`, `default_neighbor_lines`, `selection_reason` (defaulting to false/0/"" when absent).
- `RawRetrievalFile` gained `include_ast_skeleton`, `include_retriever_summary`, `include_entire_file` overrides; when absent they derive from `default_evidence_mode` via `defaultIncludeFlags` (`whole_file` → all true; `spans`/`summary+ast` → ast+summary only; `summary` → summary only; `exclude` → all false).
- `RawRetrievalOutput` gained `include_cross_file_findings`, `include_gaps`, `include_followup_queries` overrides (defaulting to `length > 0` / `false`).
- `normalizeFile` returns `{ file, includeFlags }` so `buildRecommendedEvidence` can filter to selected-tier non-exclude files and emit per-symbol spans for `selected_by_default=true` symbols using the canonical file_id/symbol_id assigned during normalization. Reserve-tier and `exclude`-mode files are deliberately absent from `recommended_evidence` — they require explicit conductor override to enter the plan.
- Path and line-number enforcement preserved; `raw_content` still stripped; discriminated `NormalizeResult` unchanged.

`src/retriever/worker.ts` propagates the new fields end-to-end:
- `candidateToRawFile` sets per-file include flags from `includeFlagsForMode(evidenceModeHint)` and marks the scout's top symbol as `selected_by_default=true` with `default_neighbor_lines=3` when the file is selected-tier in `spans`/`whole_file` mode with a sufficiently scored top symbol.
- `agentSymbolToRaw` / `agentFileToRawFile` / `agentToRawOutput` carry through the new agent-authored selection fields (including the three top-level include flags) without leaking any raw file bodies.

No schema version bump required — `src/artifacts/schemas.ts` already validated the recommended_evidence shape from AR-P1-T1/T2.

**AR-P4-T4 — retriever-agent tests**
New `tests/retriever/agent.test.ts` (10 tests) uses `runScout` against real temp repos plus a queue-backed `AgentModelCallback` to exercise the bounded agent loop without any pi-host dependency:
- `maxRounds` cap honoured even when the model refuses to stop (`stopReason: round_cap`, scout-synthesized fallback).
- `maxActionsPerRound` drops extras.
- `read_file` flows through the executor and returns the expected line slice with `totalLines`, bounded by 1-indexed `start`.
- `follow_imports` resolves `./…` specifiers inside the repo and leaves external packages unresolved.
- Stop reasons: `agent_stopped` on valid recommendation, `action_cap` on empty continue, `model_error` when the callback throws, `parse_error` on non-JSON output.
- False-positive rejection: agent drops a distracting scout candidate and cites the reason in `strategy_summary`.
- `sanitizeRecommendation` removes an out-of-repo `/etc/passwd` entry and records a warning.

`tests/retriever/retrieval-index.test.ts` grew two groups:
- `normalizeRetrievalOutput recommended_evidence`: asserts include-flag derivation per mode, reserve-tier omission, exclude-mode omission, `whole_file` mode, and the top-level include-flag defaulting/override rules.
- `runRetrieverWorkerDetailed with stubbed model`: runs scout-only and agent-driven worker passes against a three-file fixture with two distractor handlers; confirms the agent-driven selected set is a strict subset of scout's, the real handler wins, and the downstream normalized artifact's `recommended_evidence` contains exactly one file with its default spans (`include_span=true`, `neighbor_lines=2`).

**Verification**: `bunx tsc --noEmit` clean. `bun test` 411 pass / 0 fail (up from 395 — new tests are additive).

**Files modified**: `src/retriever/normalize.ts`, `src/retriever/worker.ts`, `tests/retriever/agent.test.ts` (new), `tests/retriever/retrieval-index.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Not done (by design)**: AR-P4-T5 and Sprint 5 tasks AR-P5-T2/T3 remain `planned` — out of active-task scope.

===== 2026-04-17T21:55:26Z | opus implement =====
Both active tasks complete.

**AR-P4-T4** — New `tests/retriever/agent.test.ts` (10 tests) covers bounded behavior (`maxRounds`, `maxActionsPerRound`), `read_file`/`follow_imports` executor flow, all five stop reasons, false-positive rejection, and out-of-repo path sanitization. `tests/retriever/retrieval-index.test.ts` gained a `runRetrieverWorkerDetailed` integration test verifying the agent-driven selected set is a strict subset of scout-only and that `recommended_evidence` carries the narrowed plan end-to-end.

**AR-P5-T1** — `src/retriever/normalize.ts` now builds `recommended_evidence` from normalized file ids, filtering to selected-tier non-exclude files and emitting per-symbol spans for `selected_by_default` symbols. Raw worker/agent outputs propagate the new include flags through `src/retriever/worker.ts`. Path/line enforcement and `raw_content` stripping preserved.

Verification: `bunx tsc --noEmit` clean; `bun test` 411 pass / 0 fail (up from 395). Task statuses flipped to `completed` in `agentic-retrieval-tasks.json` and a dated entry appended to `agentic-retrieval-dev-log.txt`. AR-P4-T5 and AR-P5-T2/T3 left as `planned` per scope.

===== 2026-04-17T22:25Z | opus implement AR-P5-T2 + AR-P5-T3 + AR-P6-T1 =====
All three active tasks complete.

**AR-P5-T2 — artifact inspection + conductor summaries**
- `src/services/artifact-inspect.ts`: `RetrievalIndexInspection` expanded with `strategy_summary`, `scout_terms`, `selected_file_count`, `reserve_file_count`, per-file `selection_tier`/`selection_reason`/`default_evidence_mode`, per-symbol `selected_by_default`/`default_neighbor_lines`/`selection_reason`, and the full `recommended_evidence` block. `inspectRetrievalIndex` populates tier counts and forwards the retriever-authored recommendation structurally. `BLOCKED_TYPES` still rejects `evidence-bundle-v1` inspection.
- `src/conductor/retrieval.ts`: unchanged API — `inspectRetrievalResult` now returns the richer payload automatically via the updated inspection type.
- `extensions/conductor-extension.ts`: `summarizeInspection` restructured around the new narrowed scope. It now shows strategy summary, scout terms, selected-file count vs reserve-file count, per-selected default_evidence_mode + selection_reason, a separate "Reserve candidates" list, and a dedicated "Recommended default evidence scope" block (files-in-default-plan, selected symbol spans, and the three include flags).

**AR-P5-T3 — fixtures + validation tests**
- `tests/fixtures/sample-artifacts/retrieval-index-v1.json`: enriched — more substantive `strategy_summary`, richer `scout_terms`, and a second symbol on the selected file with `selected_by_default=false` to exercise the "selected-file-but-not-default-span" case. Reserve-tier example preserved and symbol ids deduplicated (s1/s2/s3).
- `tests/retriever/normalize.test.ts` "no raw file content" golden test now also feeds `strategy_summary`, `scout_terms`, selection tiers, default evidence modes, and a per-symbol `selected_by_default`. Assertions extended to cover: new top-level strategy/scout fields, per-file tier + mode round-trip, `recommended_evidence` filters reserve-tier out, `include_span`/`neighbor_lines` propagation (2 in this test), and `include_cross_file_findings`/`include_gaps`/`include_followup_queries` defaulting rules.
- `tests/conductor/retrieval-stage.test.ts`: added "surfaces strategy metadata, tiers, default modes, and recommended scope" test that builds a selected+reserve fixture, confirms selected/reserve counts, per-file tier and default mode, per-symbol `selected_by_default`/`default_neighbor_lines`, `recommended_evidence` content (selected only, correct neighbor_lines), and no `raw_content` leakage in the serialized inspection payload.

**AR-P6-T1 — createRecommendedEvidencePlan helper**
- `src/conductor/evidence-plan.ts`: added `createRecommendedEvidencePlan(retrieval_index, options?)` with `CreateRecommendedEvidencePlanOptions` (`promote_reserve`, `assembly_options`, `prompt_sections`, `target_task`). The helper copies `recommended_evidence.files` into `selection.files` byte-for-byte (include flags + spans + neighbor_lines preserved), validates that every `file_id`/`symbol_id` exists in the artifact, and carries `include_cross_file_findings`/`include_gaps`/`include_followup_queries` straight through. Reserve-tier files are excluded by default; `promote_reserve` takes reserve `file_id`s and adds summary-only plan entries (it refuses ids that aren't reserve-tier and ids not present in the artifact). Defaults for assembly/prompt/target match `createEvidencePlan`.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 412 pass / 0 fail (up from 411 — net +1 new conductor retrieval-stage test).

**Files modified**: `src/services/artifact-inspect.ts`, `src/conductor/evidence-plan.ts`, `extensions/conductor-extension.ts`, `tests/fixtures/sample-artifacts/retrieval-index-v1.json`, `tests/retriever/normalize.test.ts`, `tests/conductor/retrieval-stage.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Not done (by design)**: AR-P6-T2/T3 and AR-P7-* remain `planned` — integrating the new default plan helper into the extension evidence stage (AR-P6-T2) and replacing the naive `createDefaultEvidencePlan` there is next-sprint scope. The conductor extension still uses `createEvidencePlan` with its summary+first-two-spans heuristic until AR-P6-T2.

===== 2026-04-17T22:02:02Z | opus implement =====
All three active tasks are complete. Verified with `bunx tsc --noEmit` (clean) and `bun test` (412 pass / 0 fail, up from 411).

## Summary

**AR-P5-T2 — inspection + conductor summaries**
- `src/services/artifact-inspect.ts`: `RetrievalIndexInspection` now exposes `strategy_summary`, `scout_terms`, `selected_file_count`, `reserve_file_count`, per-file `selection_tier`/`selection_reason`/`default_evidence_mode`, per-symbol `selected_by_default`/`default_neighbor_lines`/`selection_reason`, and the full `recommended_evidence` block. Evidence-bundle inspection stays blocked.
- `extensions/conductor-extension.ts`: `summarizeInspection` rewrites the Stage 3 notification around strategy summary, scout terms, a dedicated "Selected files" list (with default_evidence_mode + selection_reason), a "Reserve candidates" list, and a "Recommended default evidence scope" block (plan file count, selected symbol spans, the three include flags).

**AR-P6-T1 — createRecommendedEvidencePlan helper**
- `src/conductor/evidence-plan.ts`: added `createRecommendedEvidencePlan(retrieval_index, options?)` that copies `recommended_evidence.files` into `selection.files` exactly (include flags, spans, neighbor_lines), validates every `file_id`/`symbol_id` against the artifact, carries `include_cross_file_findings`/`include_gaps`/`include_followup_queries` through unchanged, and excludes reserve-tier files unless passed via `promote_reserve`. Naive `createEvidencePlan` is left in place for callers that still need explicit controls.

**AR-P5-T3 — fixtures + tests**
- `tests/fixtures/sample-artifacts/retrieval-index-v1.json`: enriched strategy summary + scout terms; selected file now has a second symbol with `selected_by_default=false` to exercise the non-default-span case.
- `tests/retriever/normalize.test.ts`: the "no raw file content" golden now also feeds strategy/scout/tiers/modes/per-symbol default-span data and asserts no leakage plus correct recommended_evidence filtering (reserve excluded, neighbor_lines respected, include-flag defaults).
- `tests/conductor/retrieval-stage.test.ts`: new "surfaces strategy metadata, tiers, default modes, and recommended scope" test covers the expanded inspection payload end-to-end and confirms `raw_content` still never appears.

**Files modified**: `src/services/artifact-inspect.ts`, `src/conductor/evidence-plan.ts`, `extensions/conductor-extension.ts`, `tests/fixtures/sample-artifacts/retrieval-index-v1.json`, `tests/retriever/normalize.test.ts`, `tests/conductor/retrieval-stage.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Not done (by design)**: AR-P6-T2 (wiring the new helper into the extension evidence stage), AR-P6-T3, and AR-P7-* remain `planned`. The extension still uses the legacy `createDefaultEvidencePlan` / `createEvidencePlan` summary+first-two-spans heuristic until AR-P6-T2.

===== 2026-04-17 | opus implement AR-P6-T2 + AR-P6-T3 =====
Both active Sprint 6 tasks complete.

**AR-P6-T2 — extension uses retriever-authored default plan**
- `extensions/conductor-extension.ts`: swapped the naive summary+first-two-spans `createDefaultEvidencePlan` body for a call to `createRecommendedEvidencePlan(index, { target_task })`. The function name is preserved so the Stage 4 flow (build plan → store → notify → preview → confirm → materialize) stays intact. Import swapped from `createEvidencePlan` to `createRecommendedEvidencePlan`.
- `summarizeEvidencePlan` rewritten to show: "Retriever-authored default plan (narrowed from N retrieved, R held as reserve)", per-file `[default_evidence_mode] — whole/ast/summary/spans=N` (or "exclude"), and the three top-level include flags. Conductor inspection of the plan before materialization is unchanged.

**AR-P6-T3 — budget helper + test alignment**
- `src/util/budget.ts`: added `estimateRecommendedBudget(index, assembly_options?)` that constructs a synthetic EvidencePlanV1 mirroring the retriever-authored default scope (copies `recommended_evidence.files` + the three include flags, clamps `neighbor_lines` to non-negative integers, uses sane DEFAULT_ASSEMBLY_OPTIONS_FOR_RECOMMENDATION) and delegates to `estimateBudget`. Pure util — no import from `src/conductor/`.
- `tests/assembler/evidence-plan.test.ts`: added `createRecommendedEvidencePlan` suite (13 tests) covering: recommendation→plan mapping (include flags + span + neighbor_lines), reserve-tier exclusion, include_cross_file/gaps/followup passthrough, narrowness vs `createEvidencePlan`, reserve promotion (happy path + no-op for already-recommended id), validation failures (unknown promoted id, non-reserve promoted id, unknown recommended file_id, unknown recommended symbol_id), target_task/assembly_options overrides, and embedded-index byte-equality.
- `tests/assembler/budget.test.ts`: new `estimateRecommendedBudget` suite (5 tests): recommendation-derived scope (includes only f1 with skeleton=3 + summary + span=24 lines for s1 with neighbor=2), narrower than the legacy summary-for-all plan (fewer files, fewer lines/tokens), assembly_options overrides respected, determinism, and empty-recommendation → zero-lines.
- `tests/assembler/evidence-assembler.test.ts`: added end-to-end test that marks f2 reserve/exclude, populates recommended_evidence with f1 + one default span (neighbor=1), calls `createRecommendedEvidencePlan`, and verifies the materialized bundle contains exactly `main.ts` in both structural context and raw evidence — reserve file never appears.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 432 pass / 0 fail (up from 412 — 20 net new tests).

**Files modified**: `extensions/conductor-extension.ts`, `src/util/budget.ts`, `tests/assembler/evidence-plan.test.ts`, `tests/assembler/budget.test.ts`, `tests/assembler/evidence-assembler.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Not done (by design)**: AR-P7-* (narrow override API) and AR-P8-* (docs + end-to-end regression) remain `planned` — out of active-task scope.

===== 2026-04-17T22:09:04Z | opus implement =====
Both active Sprint 6 tasks are done. `bunx tsc --noEmit` clean; `bun test` 432 pass / 0 fail (up from 412).

**AR-P6-T2 — extension uses retriever-authored default plan**
- `extensions/conductor-extension.ts:397`: `createDefaultEvidencePlan` now calls `createRecommendedEvidencePlan(index, { target_task })` — naive summary+first-two-spans heuristic gone. Function name preserved so Stage 4 flow is unchanged.
- `summarizeEvidencePlan` rewritten to show "narrowed from N retrieved, R held as reserve", per-file `[mode] — whole/ast/summary/spans=N` lines, and the three include flags. Conductor still inspects the plan between creation and materialization.

**AR-P6-T3 — budget helper + test alignment**
- `src/util/budget.ts`: added `estimateRecommendedBudget(index, assembly_options?)` that builds a synthetic plan from `recommended_evidence` and delegates to `estimateBudget`.
- `tests/assembler/evidence-plan.test.ts`: +13 tests covering `createRecommendedEvidencePlan` (mapping, reserve exclusion, promotion, validation failures, overrides).
- `tests/assembler/budget.test.ts`: +5 tests for `estimateRecommendedBudget` (narrower than summary-for-all, determinism, empty-recommendation).
- `tests/assembler/evidence-assembler.test.ts`: +1 end-to-end test confirming a materialized recommendation-derived bundle excludes the reserve file.

**Files modified**: `extensions/conductor-extension.ts`, `src/util/budget.ts`, `tests/assembler/evidence-plan.test.ts`, `tests/assembler/budget.test.ts`, `tests/assembler/evidence-assembler.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

No unfinished work in scope. AR-P7-* and AR-P8-* remain `planned`.

===== 2026-04-17 | opus implement AR-P7-T1 + AR-P7-T2 + AR-P7-T3 =====
All three active Sprint 7 tasks complete — narrow, deterministic conductor override API for evidence plans.

**AR-P7-T1 — evidence-overrides.ts**
- New `src/conductor/evidence-overrides.ts` with discriminated `EvidenceOverride` union covering `promote_file`, `demote_file`, `set_file_mode`, `include_symbol`, `exclude_symbol`, `set_neighbor_lines`, and the three `toggle_*` selection flags.
- `applyEvidenceOverrides({ plan, retrieval_index, overrides })` deep-clones the input plan, applies each operation in order, validates every file_id and symbol_id against the retrieval artifact, and returns a fresh `evidence-plan-v1` with a new `artifact_id` plus an `applied[]` audit trail. Input plan is never mutated. Any invalid reference (unknown file, unknown symbol, promoting an already-planned file, demoting a file not in the plan, tuning a span that doesn't exist, negative neighbor_lines) throws deterministically — short-circuiting the entire override list so the source plan stays byte-identical.
- Shared `modeToIncludeFlags(mode: RetrievalDefaultEvidenceMode)` helper matches `normalize.ts`/`worker.ts` semantics so override modes align with the retriever's own vocabulary. Promoting an `exclude`-mode reserve file defaults to `summary`-mode flags so promotion can never add an empty plan entry.

**AR-P7-T2 — extension wiring**
- `extensions/conductor-extension.ts`: Stage 4 now starts from `createDefaultEvidencePlan(index)` (retriever-authored), persists it, shows selected + reserve + recommendation state via `summarizeEvidencePlan`, then prompts the conductor to optionally apply narrow overrides. If the user says yes and provides a JSON array, `parseEvidenceOverridesInput` validates each op shape, `applyEvidenceOverrides` adjusts the plan (validated against the retrieval artifact), the adjusted plan is stored, and `evidence_plan_id` is rebound. Preview + materialize run against the adjusted plan.
- Failure mode: on parse or validation error, the user sees `Overrides rejected — keeping retriever defaults.` and the flow continues with the retriever-authored default. No raw-file read capability is introduced — overrides work purely over structural plan + retrieval-index metadata.
- `summarizeEvidencePlan` now shows `Selected files (in default plan)` and `Reserve candidates (not in default plan)` as distinct lists so the conductor review output clearly distinguishes selected vs reserve vs adjusted.
- New `OVERRIDE_HELP` text + `VALID_OVERRIDE_OPS` guard keep the CLI help self-describing and reject typos before reaching `applyEvidenceOverrides`.

**AR-P7-T3 — tests**
- New `tests/assembler/evidence-overrides.test.ts` (33 tests). Success paths cover `modeToIncludeFlags` exhaustively, artifact_id regeneration, input-plan immutability, change isolation (unchanged fields/files remain byte-equal), reserve promotion with and without explicit mode, `exclude`-mode reserve promotion falling back to `summary`, `demote_file`, `set_file_mode`, `include_symbol` (new + re-enable + neighbor_lines update + fractional clamp), `exclude_symbol` removing the span, `set_neighbor_lines` on existing spans, and the three toggle flags. Multi-op sequencing (`promote_file` then `include_symbol`) proves ordering is deterministic. A schema round-trip test confirms the adjusted plan still validates.
- Failure paths: unknown file_id / symbol_id for every op, promoting a file already in the plan, demoting a file not in the plan, `set_file_mode` on a file not in the plan, `include_symbol`/`exclude_symbol`/`set_neighbor_lines` on files/symbols not currently in the plan, negative `neighbor_lines`, and first-invalid-op rejection preserving the source plan byte-for-byte.
- `tests/assembler/evidence-plan.test.ts`: +1 integration test confirming `createRecommendedEvidencePlan` output is directly consumable by `applyEvidenceOverrides` and reserve files can be promoted via the override path.

**Verification**: `bunx tsc --noEmit` clean. `bun test` 466 pass / 0 fail (up from 432 — 34 net new tests).

**Files modified**: `src/conductor/evidence-overrides.ts` (new), `extensions/conductor-extension.ts`, `tests/assembler/evidence-overrides.test.ts` (new), `tests/assembler/evidence-plan.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

**Not done (by design)**: AR-P8-* (docs + end-to-end regression) remain `planned` — out of active-task scope.

===== 2026-04-17T22:17:11Z | opus implement =====
All three AR-P7 tasks are complete.

## Summary

**AR-P7-T1 — `src/conductor/evidence-overrides.ts` (new)**
Narrow, deterministic override API with a discriminated `EvidenceOverride` union: `promote_file` / `demote_file` / `set_file_mode` / `include_symbol` / `exclude_symbol` / `set_neighbor_lines` / `toggle_cross_file_findings` / `toggle_gaps` / `toggle_followup_queries`. `applyEvidenceOverrides` deep-clones the input plan, validates every file/symbol reference against the retrieval artifact, applies ops in order, and returns a fresh plan with a new `artifact_id` plus an `applied[]` audit trail. Invalid references throw and short-circuit the whole list, leaving the source plan byte-identical. Shared `modeToIncludeFlags` matches the retriever's normalize/worker vocabulary.

**AR-P7-T2 — `extensions/conductor-extension.ts`**
Stage 4 now starts from the retriever-authored default, presents selected vs reserve distinctly, prompts the conductor to optionally apply a JSON array of narrow overrides, adjusts the plan, re-stores it, and only then previews/materializes. Parse or validation errors surface as a warning and the flow falls back to the retriever default — no raw-file access added. New `OVERRIDE_HELP` + `VALID_OVERRIDE_OPS` guard self-describe the CLI interface.

**AR-P7-T3 — tests**
- New `tests/assembler/evidence-overrides.test.ts`: 33 tests covering modeToIncludeFlags, success paths (artifact_id regeneration, input-plan immutability, change isolation, reserve promotion with/without mode, `exclude`→`summary` fallback, demote, set mode, include/exclude/set neighbor_lines with fractional clamp, multi-op sequencing, schema round-trip, no-op list) and failure paths (unknown file/symbol, already-planned promote, not-in-plan demote, negative neighbor_lines, first-invalid-op preserves source).
- `tests/assembler/evidence-plan.test.ts`: +1 integration test confirming `createRecommendedEvidencePlan` output is directly consumable by `applyEvidenceOverrides`.

**Verification**: `bunx tsc --noEmit` clean. `bun test` **466 pass / 0 fail** (up from 432 — 34 net new tests).

**Files modified**: `src/conductor/evidence-overrides.ts` (new), `extensions/conductor-extension.ts`, `tests/assembler/evidence-overrides.test.ts` (new), `tests/assembler/evidence-plan.test.ts`, `agentic-retrieval-tasks.json`, `agentic-retrieval-dev-log.txt`.

No unfinished work in scope. AR-P8-* (docs + end-to-end regression) remains `planned`.
